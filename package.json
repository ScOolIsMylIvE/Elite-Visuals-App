{
  "name": "elite-visuals",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "echo 'no tests'"
  },
  "devDependencies": {
    "turbo": "^1.10.6",
    "typescript": "^5.3.3"
  }
}
